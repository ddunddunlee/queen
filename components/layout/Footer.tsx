export function Footer() {
  return (
    <footer className="border-t border-line">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-6 text-sm text-slate-400 sm:flex-row sm:items-center sm:justify-between">
        <span>Built for repeatable game-ready pixel sprite workflows.</span>
        <span className="font-mono text-xs">MVP: prompts + canvas export + metadata</span>
      </div>
    </footer>
  );
}
